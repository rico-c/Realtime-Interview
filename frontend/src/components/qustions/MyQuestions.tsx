import React, { FC, useCallback } from 'react';
import { Tabs } from 'antd';

const { TabPane } = Tabs;

const MyQuestions: FC = () => {
  return (
    <div className="my-questions">
      123
    </div>
  );
};

export default MyQuestions;
